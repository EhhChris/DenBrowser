# Firefox source and build artifacts
/src/

# macOS
.DS_Store

# Build logs
*.log

# Rust build artifacts
proxy/target/

# Per-deployment proxy runtime config (copy proxy.example.toml -> proxy.toml).
# Kept out of git so machine-specific paths / settings aren't committed; the
# tracked template is proxy/proxy.example.toml.
proxy/proxy.toml

# Attestation key material — NEVER commit these
build/
test/**/*private*.pem
test/**/*.key
